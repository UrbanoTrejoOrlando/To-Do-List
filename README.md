# React + Vite

Este es un proyecto que utiliza React y Vite para la creación de una aplicación web.

## Instalación de dependencias

Para instalar las dependencias necesarias para este proyecto, ejecuta los siguientes comandos:

```bash
npm install bootstrap
npm install bootstrap-icons
npm install sweetalert2
